{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Reset local web ports",
      "type": "shell",
      "command": "powershell",
      "args": [
        "-NoProfile",
        "-Command",
        "$ports = 5500, 8000; foreach ($port in $ports) { $listeners = Get-NetTCPConnection -LocalPort $port -State Listen -ErrorAction SilentlyContinue; foreach ($listener in $listeners) { Stop-Process -Id $listener.OwningProcess -Force -ErrorAction SilentlyContinue } }"
      ],
      "problemMatcher": []
    },
    {
      "label": "Start static Web UI (5500)",
      "type": "shell",
      "command": "${workspaceFolder}\\.venv\\Scripts\\python.exe",
      "args": ["-m", "http.server", "5500", "--directory", "web_ui"],
      "dependsOn": "Reset local web ports",
      "dependsOrder": "sequence",
      "isBackground": true,
      "problemMatcher": {
        "owner": "local-web-ui",
        "pattern": [
          {
            "regexp": "^$"
          }
        ],
        "background": {
          "activeOnStart": true,
          "beginsPattern": "Serving HTTP on",
          "endsPattern": "Serving HTTP on"
        }
      }
    }
  ]
}
